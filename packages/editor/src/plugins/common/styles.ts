import { PluginStyleFn } from '../../types';

// TODO: fix colors
export const styleFunction: PluginStyleFn = ({ constants }) => `
  .saruni-ui-selection-blur-marker {
    background-color: red;
  }
`;
