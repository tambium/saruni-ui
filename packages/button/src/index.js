import styled from '@emotion/styled';

const Button = styled.button`
  background: navy;
  color: #fff;
  border-radius: 4px;
  cursor: pointer;
  font-size: 1rem;
  font-weight: 500;
  padding: 8px 32px;
`;

export default Button;
